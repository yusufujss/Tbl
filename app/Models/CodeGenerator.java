package models;
import java.util.*;
import java.math.*;

public class CodeGenerator
{
/**
	String code;
	
	public CodeGenerator (String code)
	{
		// initialize constructor
		this.code = code;
	}
	
		public static void codeGenerator()
		{
			for (int i=0; i<=1000; i++)
			{
				code = random(); // incomplete
				// there is a need to verify the process of saving by ensuring that the server is available
				save(code);
			}
			System.out.printf("ALL CODES ARE GENERATED AND SAVE SUCCESSFULLY");
		}

**/
}
